# Hawk browser error reporting (https://docs.hawk-tracker.ru/react)
# Vite reads VITE_* from this file OR from repo root `.env` (see vite.config envDir).
# Restart `npm run dev` after changes. Production: rebuild `npm run build`.
# VITE_HAWK_TOKEN=
# VITE_HAWK_RELEASE=0.1.0
